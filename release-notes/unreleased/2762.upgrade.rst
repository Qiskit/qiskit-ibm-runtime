The ``RuntimeJobV2.result()`` and ``RuntimeJobV2.wait_for_final_state()`` methods now accept an
optional ``poll_interval`` parameter that allows customizing the interval between request to the
service. The default is now ``0.5`` seconds for non-session job, and ``0.1`` seconds for session
jobs.
