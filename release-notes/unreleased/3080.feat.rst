:class:`~.IBMBackend` now has a ``physical_qubits`` attribute, which returns the number of physical
qubits of the backend. The attribute is also available in :class:`~.FakeBackendV2` for backends
that are not retired.
