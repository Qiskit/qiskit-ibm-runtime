The method `.accounts.CloudAccount.resolve_crn ` did not inject proxy and ssl_verification
arguments to outgoing request. Added the arguments to `GlobalSearchV2`, `GlobalCatalogV1` 
and `IAMAuthenticator` services. Also debbugued integration tests for proxy usage.