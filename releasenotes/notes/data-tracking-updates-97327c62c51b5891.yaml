---
features:
  - |
    A new environment variable, ``USAGE_DATA_OPT_OUT`` can be used to 
    opt out of user module usage tracking. Users can also use the new parameter,
    ``data_tracking`` in :meth:`qiskit_ibm_runtime.QiskitRuntimeService.save_account`
    to opt out.
